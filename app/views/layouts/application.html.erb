<!DOCTYPE html>
<html>
<head>
  <title>Business Cards Organizer</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <meta name="viewport" content="initial-scale=1.0, user-scalable=no" />

  <script type="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyA6Cb5j7ll1l-WDHDG8vPjnfZxJpd5I_cI">
  </script>

</head>
<body>
  <header>
    <h1>Welcome to the Future - Business Cards Organizer</h1>
  </header>

  <% if current_user %>

    <div class="img" id="me">

      <% if current_user.image_url %>
      <img id="my_avatar" src="<%= current_user.image_url %>" alt="Profile Picture" />
      <% else %>
        <img id="my_avatar" src="<%=asset_url('navatar.jpg')%>" alt="Profile Picture" />
      <% end %>

    <%= current_user.email %> is logged in.
    <%= link_to "LogOut", logout_path, method: :delete %>
      <% else %>
        <%= link_to 'SignUp', signup_path %> or
        <%= link_to "Sign in with Linkedin", "/auth/linkedin", id: "sign_in" %> or
        <%= link_to 'LogIn', login_path %>
    </div>

  <% end %>


<div class="navigation-bar">
  <button class="navbar show-cards">Show Saved Cards</button>
  <button class="navbar hide-cards">Hide Saved Cards</button>
  <button class='navbar show-contacts'>Show Contacts</button>
  <button class='navbar hide-contacts'>Hide Contacts</button>
  <button class='navbar show-contacts-on-map'>Show Contacts on Map</button>
  <button class='navbar hide-contacts-on-map'>Hide Contacts on Map</button>
</div>


  <%= yield %>

</body>
</html>
