
old version of UsersController # create
=======================================

  def create
    @user = User.new(user_params)
    if @user.save
      redirect_to login_path #profile_path
    else
      render :new
    end
  end















